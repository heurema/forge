"""Vendored rubric snapshot — populated by `forge doctor --refresh-rubric`.

This stub allows `import forge.rubric_snapshot` to succeed even before
the rubric is vendored. audit.py checks manifest.json for validity
before calling any rubric functions.
"""
